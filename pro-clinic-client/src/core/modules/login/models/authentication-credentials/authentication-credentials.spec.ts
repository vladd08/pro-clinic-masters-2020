import { AuthenticationCredentials } from './authentication-credentials';

describe('AuthenticationCredentials', () => {
  it('should create an instance', () => {
    expect(new AuthenticationCredentials()).toBeTruthy();
  });
});
